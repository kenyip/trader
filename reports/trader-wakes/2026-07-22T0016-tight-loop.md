# WAKE — 2026-07-22T0016 tight-loop architecture

WAKE: 2026-07-22 ~00:16 PDT  
PHASE: PAPER + BUILD  
CHOSE: Implement Ken's faster architecture — non-stop parallel programmatic worker + smarter LLM coach/RTH

## DID

1. `scripts/trader_quality_cycle.py` — one cycle with parallel stress/multi after serialized evolves
2. `scripts/trader_quality_worker.sh` — non-stop loop (start/stop/status/ensure); sleep 20s between cycles
3. Cron `trader-quality-worker` `*/10` ensure; worker **started** pid live
4. Autonomous tick defers residual when worker alive (no double work)
5. LLM prompts: continuum-judgment 9/15/21 coach worker results; rth-eval = opportunity + paper manage
6. `just trader-status` shows quality_worker=ON; doctrine TRADER_CRON_LAYOUT updated

## ARCHITECTURE

Programmatic muscle (tight/parallel) ← worker  
LLM coach + RTH manage ← agent crons  
Ken only: gateway / arm / $3k

## NOT

- Not 5m densify thrash
- Not live / arm
- Not LLM on every cycle

## NEXT

Worker keeps cycling. LLM coaches at 9/15/21 and RTH :30. Ken optional: `just trader-status`.

## VERIFICATION

- just trader-quality-worker status → RUNNING
- just trader-status → quality_worker=ON
