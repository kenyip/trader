# Merged NEXT seed — 2026-07-12T1806

## ONE NEXT

Inventory no-paid historical corporate-action sources that can supply **both** ex-date and honest announcement-time provenance (`known_at`). Implement an archived `DividendEventProvider` **only if** `known_at` can be represented without fabricating announcement dates from ex-dates. Otherwise write a fail-closed data decision packet and keep required-mode diagonal/bull-call simulations blocked.

## Constraints

- Paper/research only; no live, shadow auto-promote, broker login, or agentic arm.
- Proxy BS marks remain L0; this provider enables claim-honest required mode, not automatic edge.
- Empty list ≠ missing coverage (`None` / missing provider is fail-closed under required mode).
- Do not backfill `known_at` from ex-date alone.
- Zero-input next wake may supersede this seed if inventory yields a hard data block and a different open historical-proxy or capability loop is higher-information — state why.
- Parallel only on a distinct NY RTH date: archive append 1→2/3 (orientation data dependency, not this loop’s seat).

## Closed this wake (context, not allowlist)

Announcement-time dividend + short-call extrinsic assignment boundary for diagonal and bull-call debit sims, with fail-closed required mode and bear-put isolation. Prior rejected daily-bar signal families remain closed without new evidence class.
