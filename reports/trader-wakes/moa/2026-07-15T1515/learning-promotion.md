# Learning promotion — 2026-07-15T1515

Strategy charter/outcome: gradual information diffusion and trend-following demand were hypothesized to create fixed-ten-session underlying continuation after a completed close at least 2% above the prior completed 20-session high while above a fully warmed prior-completed SMA100. Frozen scope was `MULTINAME_BREAKOUT_BULL_CALL_14D_V1` / `TIME_SERIES_20D_BREAKOUT_CONTINUATION`: AAPL/MSFT/NVDA/AMZN/META/GOOGL/AMD/TSLA adjusted closes, next-session entry, prior-only same-symbol near-high controls matched without replacement, non-overlapping windows, chronological 60/40 blueprint split, labeled symmetric 20-bps absolute-level underlying sensitivity, and one-sided 90% five-pair-block bootstrap lower bound. A future one-lot 14-DTE `$1`-wide bull-call debit spread is structural context only (`capital_fit_usd=max_loss_usd=100`, `max_lots=1`, before closing friction).

Outcome: `BLOCKER_REMOVED_AND_RETESTED`; retest `STRATEGY_ADVANCED`; `F0_MECHANISM → F1_TRAIN` only. The prior `2026-07-15-viable-path` epoch's mandatory three-wake no-advance burst stop was resolved by an accurate written reassessment, a successor epoch, and a materially different prior-only within-symbol event-study capability exercised in the same wake. The frozen train population passed all six gates: 281 outcome-independent blueprints; train 168 across eight symbols; treated `+1.6318%` after the 20-bps label; controls `+0.0785%`; paired excess `+1.5533%`; LB90 `+0.2947%`; integrity zero. The final 113 remain outcome-unread; `pricing_calls=0`. No option PnL, managed-path, L1, capital seat, registry, paper, shadow, arm, broker, funding, or live authority exists.

Accepted challenger findings: preserve the narrow pooled F1 advance while explicitly rejecting universal-by-symbol, temporal-uniformity, TSLA-independence, holdout-validation, and option-payoff interpretations; make concentration reports mandatory but non-gating beside the one-shot holdout; distinguish the fixed-ten-session measured outcome from untested invalidation/50%-harvest management; label 10-session underlying versus 14-DTE option alignment as approximate; state that symmetric 20 bps cancels from paired excess and is not option-cost robustness; add a thin-symbol-breadth negative control; and independently rerun focused/full/smoke/coverage verification. The finalizer also repaired complete-diff defects not caught by the challenger: the reassessment misnamed the prior epoch and described the wrong three prior closes, and the replacement epoch config had renamed canonical bar fields, used `reassessment_path` instead of `reassessment_doc`, and dropped durable orientation/policy keys.

Rejected interpretations: no retroactive concentration hard gate on inspected train rows; no managed option expectancy; no differential-cost, option-fill, or option-mispricing claim; no broad-panel generalization; no L1/capital seat/paper eligibility; no reopening prior closed families; no archive freeze; no shadow/arm/live. Challenger N6 was an independent-verification requirement rather than a code defect and is closed by the commands below. N7 required no repair because the merged NEXT already freezes the 281-blueprint geometry, opens the reserved 113 exactly once, keeps concentration diagnostics non-gating, and prohibits option pricing unless F2 advances.

## VERIFICATION

- `.venv/bin/python -m unittest -v tests.test_breakout_continuation_train_lab` -> `Ran 8 tests in 0.398s`, `OK`. Behavioral/boundary/negative controls cover outcome-shock selection invariance, chronology and prior-only matching, positive gates, positive-point/non-positive-bootstrap rejection, zero support strict nulls, nonfinite fail-close, unread holdout, zero option pricing, claim semantics, and the new one-symbol breadth failure despite otherwise passing return/bootstrap gates.
- `.venv/bin/python -m unittest -v tests.test_breakout_continuation_train_lab tests.test_low_hv_cross_section_train_lab tests.test_tsll_tracking_dislocation_train_lab tests.test_monthly_opex_post_expiry_train_lab` -> `Ran 29 tests in 1.186s`, `OK`.
- `.venv/bin/python -m py_compile scripts/breakout_continuation_train_lab.py tests/test_breakout_continuation_train_lab.py` -> exit `0`.
- Two unchanged-source lab invocations regenerated canonical and rerun cache payloads -> both `STRATEGY_ADVANCED`, train 168, paired excess `0.015533019634700258`, LB90 `0.002946598023672219`, holdout 113 unread, `pricing_calls=0`; complete substantive equality excluding only `generated_at` plus concentration/boundary assertions -> `SUBSTANTIVE_REPRODUCTION_AND_BOUNDARIES_OK train=168 holdout=113 pricing=0`; final canonical SHA-256 `2ea7d7f27cb3dc664bfd5e54be0ac4ec263fca9bdae0a388a4726093a9723036`.
- `.venv/bin/python scripts/trader_income_coverage.py --stamp 2026-07-15T1532` plus dated/LATEST `cmp` -> exit `0`, `COVERAGE_SURFACES_IDENTICAL`; 21 structures, 246 hypotheses, 70 evolve artifacts, living leader none.
- `just platform-smoke` -> exit `0`, `platform smoke OK`; `agentic_live` remained blocked at the Robinhood Stage1 OAuth gate.
- `.venv/bin/python -m unittest discover -s tests` -> `Ran 312 tests in 14.239s`, `OK`.

## DURABLE

Every material challenger finding is reconciled:

1. `scripts/breakout_continuation_train_lab.py` and `tests/test_breakout_continuation_train_lab.py` now provide reusable prior-only event-time matching, chronology/no-reuse/non-overlap integrity, strict train/unread-holdout separation, uncertainty gates, zero-support strict nulls, minimum-symbol breadth, and machine-readable claim boundaries for fixed horizon versus untested management, approximate 10-session/14-DTE alignment, symmetric-cost cancellation, and mandatory non-gating holdout concentration diagnostics.
2. `docs/SEARCH_DESIGN_REASSESSMENT_2026-07-15T1515.md` accurately names prior epoch `2026-07-15-viable-path` and its tracking-dislocation, one-risk-unit cadence, and monthly-OPEX dispositions. It labels yfinance's requested end as exclusive and the realized panel end as 2026-07-14.
3. `configs/search_epoch.json` preserves canonical orientation/policy keys (`status`, `reassessment_doc`, `charter_doc`, `goal_doc`, `discovery_bar`, `capital_seat_bar`, `ken_soft_research_prior`, `epoch_success_definition`, `do_not`) while freezing exactly one F1 holdout continuation path and quarantining prior families.
4. Final merge/LATEST/INDEX/readiness/coverage/NEXT and schema-v2 compounding surfaces agree on `BLOCKER_REMOVED_AND_RETESTED`, retest `STRATEGY_ADVANCED`, F0→F1/L0 only, pooled/concentrated evidence, holdout unread, option pricing zero, structural `$100`/one-lot-only context, living leader none, capital path empty, and exactly one holdout seed.
5. The profile-local `trader-self-evolution` skill now records the reusable epoch-reassessment pitfall: reconcile the actual integrated prior epoch and preserve orientation-consumed config keys instead of copying stale diagnoses or silently renaming fields. Profile memory is unchanged because this is dated project truth and procedure, not a stable Ken preference or routing fact.

Dated outcome/current project truth is promoted through the reassessment/config, executor/challenger/final wake surfaces, LATEST/INDEX/readiness/coverage, this report, and schema-v2 `compounding.json`. The cache payload remains reproducible research evidence and is deliberately not promoted to L1 or a capital seat.

Integration is pending the deterministic wrapper gate. The finalizer did not stage the real Git index, commit, push, merge, switch branches, edit `.gitignore`, place an order, or claim `RUN COMPLETE`.

## LESSON

Future Trader can separate a directional discovery pre-screen from its eventual option expression without laundering evidence: freeze signal and outcome-independent prior controls, reserve holdout blueprints before reading outcomes, enforce breadth and uncertainty, then label fixed underlying horizon, option DTE, managed exits, and cost semantics separately. A symmetric absolute-return haircut does not test paired excess, and pooled significance can coexist with material symbol/time concentration; predeclare those concentration reports for the one-shot holdout without inventing a post-hoc train gate. Future Trader also knows that a successor-epoch reassessment must be checked against integrated compounding/readiness history and must preserve canonical orientation keys, or the next zero-input wake can inherit stale/null doctrine despite a correct strategy result.

## NEXT

`BREAKOUT_UNTOUCHED_HOLDOUT_ONCE`: reconstruct the exact frozen 281 blueprints under unchanged panel, signal, match bounds, horizon, and 20-bps label; select the reserved final 113 without retuning; read their outcomes exactly once; and decide F1→F2 advance-or-`FAMILY_CLOSED` under the unchanged predeclared pooled discovery gate. Mandatorily report per-symbol treated/control/paired means and counts, leave-one-symbol-out pooled paired-excess LB90, and chronological tertiles as non-gating concentration diagnostics. Price no options unless holdout advances. No registry, seat, paper, shadow, arm, broker, or live.
