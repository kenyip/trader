# MoA challenger critique — 2026-07-15T1747 (Grok 4.5)

PARTIAL PHASE: challenger only. No evolve --apply, no broker, no arm, no commit/push/merge, no RUN COMPLETE.

## Verdict

**Accept** executor strategy outcome `FAMILY_CLOSED` for exact family `POST_SHOCK_RANGE_COMPRESSION_MATCHED_CONTROL` / candidate `POST_SHOCK_RANGE_COMPRESSION_IRON_BUTTERFLY_21D_V1` at `F0_MECHANISM -> F0_MECHANISM`.

This is an honest, predeclared F0 falsification of a genuinely new event-conditioned range-compression mechanism before any option marks. Search information rose; strategy advancement did not. No L1, registry, capital seat, paper, shadow, arm, or live claim is supported.

**Reject / supersede** executor NEXT `MONTH_END_FLOW_POSITIVE_DRIFT_PCS_F0` as written — too near already-closed calendar seasonality families. Merged NEXT is below.

Finalizer resolution: the one living seed is `POST_EARNINGS_INFO_RESOLUTION_DRIFT_F0`; detailed contract remains in `merged-next-seed.md`, `learning-promotion.md`, final merge/LATEST/INDEX, and readiness.

## Rubric

1. Strategy charter — **PASS**. Charter + closeout name economic mechanism (post-shock liquidity/attention normalize → tighter five-session close path + pin), exact family/candidate, funnel F0→F0, predeclared multi-gate falsifier, and single outcome `FAMILY_CLOSED`. Layered Edge Stack is complete for an underlying-only F0 pre-screen: forecast `range_bound`, planned symmetric `$2` 21-DTE iron butterfly, Greeks, regime, entry next-session, stand-aside, `capital_fit_usd=$200`, `max_loss_usd=$200`, `max_lots=1`, one global unit. Authority capped at F1/L0 even on pass.

2. Strategy vs operations — **PASS**. Lab, 8 tests, coverage refresh, and shared cache first-run/replay repair are search information / evidence integrity, not strategy progress. Strategy close is the train gate failure on real matched pairs, not capability theater. Not a `BLOCKER_REMOVED_AND_RETESTED` claim (and does not need to be).

3. Goal progress — **PASS (honest close)**. Wake improved the chance of a paper-testable edge by decisively killing a plausible short-gamma/theta expression before four-leg costs could invent hope. Dominant failure is path *expansion* after the shock (treated mean range 5.019% > control 4.644%; paired compression −0.375%; LB90 −1.013%; pin edge 0). Holdout remains unread; pricing_calls=0. If finalizer accepts, global no-advance streak becomes **2** (orientation started at 1 after 1648); next zero-input wake should treat `strategy_pivot_required` as true. Active epoch `2026-07-15-time-series-breakout-payoff` is already `completed`; this is free post-epoch search, not epoch thrash.

4. Creativity and independence — **PASS**. Prior NEXT class was non-monotonic theta (not bull-call). Executor did **not** reopen generic iron-butterfly cost-reject, asymmetric IC, or `MULTINAME_BREAKOUT_BULL_CALL_14D_V1`. New mechanism + prior-only same-symbol high-HV no-shock matched controls is a real evidence-class delta. Panel from current research.db run 36 rank is justified and labeled fixed present-day (survivorship/listing bias; no population-wide claim).

5. Claim validity — **PASS with nits**. Independent recompute of train metrics matches the claim JSON exactly (means, paired compression, pin rates, gate_checks, integrity `[]`). Prerequisites match an F0 underlying pre-screen: no option marks required; observed-option archive blockage correctly did not freeze the loop. Nits for finalizer labeling only (do not reopen family):
   - Horizon alignment is approximate (5-session close path/pin vs planned 21-DTE butterfly); already stated in artifact `horizon_alignment` — keep that label if any prose drifts toward “theta/vega validated.”
   - Pin is terminal ±3% only, not full-path containment — acceptable because predeclared.
   - AMD/NVDA train n=4 each is sparse; AAPL post-hoc positive n=10 must stay non-candidate (executor correctly refused salvage).
   - Control definition is high-HV *and* |1d|≤0.5% (same hv_ratio floor as treated) — correct for stated “high-HV neutral no-shock,” not a generic quiet-market control.

6. Evidence and test quality — **PASS**. Real tools/paths:
   - claim: `reports/trader-wakes/moa/2026-07-15T1747/post-shock-range-compression-train.json`
   - lab: `scripts/post_shock_range_compression_train_lab.py`
   - tests: `tests/test_post_shock_range_compression_train_lab.py` (challenger re-ran **8/8 OK**)
   - shared provenance repair: `scripts/low_hv_cross_section_train_lab.py` + strengthened exact-series test (`np.nextafter` + `check_exact=True`)
   Behavioral/negative checks include prior-only controls, outcome-change does not reselect, holdout secrecy (no pairs in holdout payload), breadth floor, flat-panel strict nulls, nonfinite fail-closed, and positive-mean/nonpositive-bootstrap fail. Population/proxy semantics labeled (`bar_claimed=discovery`, `l1_claim=false`, `registration_eligible=false`, survivorship_bias=true). Executor full-suite **339/339** not re-run here — finalizer must re-verify after any repair.

7. Falsification — **PASS**. Density/breadth/integrity passed; all four economic gates failed; all three chronological train tertiles negative (−0.623%, −0.279%, −0.230%). Quarantine scope is exact pooled panel/trigger/control/horizon family — not a universal ban on every shock or every iron butterfly. No threshold mining.

8. Capital honesty — **PASS**. No living quality leader in coverage/readiness. Structural `$200` width is planning ceiling only, not simulated/observed loss. No seat, no after-cost option edge, no B3/B4 option claim.

9. Research freedom — **PASS**. Observed-option thin archive did not freeze unrelated historical-underlying discovery. Free symbol/strategy path used; TSLA/TSLL not privileged; no diversify-for-fear seat. No removable over-restriction found in this loop.

10. ONE highest-information NEXT — **FAIL as proposed; supersede**. `MONTH_END_FLOW_POSITIVE_DRIFT_PCS_F0` sits too close to closed calendar-seasonality work already on the quarantine list:
   - `spy-turn-of-month-first-session-5day-pcs-21dte-020delta-1wide-dualcost-60-40-v1`
   - `MONTHLY_OPEX_POST_EXPIRY_BROAD_INDEX_DRIFT`
   - month-end ranking language also collides with closed `MONTHLY_CROSS_SECTION_12_1_MOMENTUM_FORWARD_DRIFT`
   With no-advance streak heading to 2, the next wake needs a **materially different economic mechanism**, not another pure calendar-flow drift screen. See merged seed.

## Disposition table

| Claim | Disposition | Note |
|---|---|---|
| `FAMILY_CLOSED` F0→F0 post-shock range compression | **ACCEPT** | Metrics + gates + integrity verified |
| No L1 / no seat / pricing_calls=0 | **ACCEPT** | |
| Holdout unread | **ACCEPT** | `outcome_metrics_read=false` |
| Cache first-run/replay provenance repair | **ACCEPT** | Shared loader; ship with wake; regression strengthened |
| New lab + 8 tests | **ACCEPT** | Challenger 8/8 green |
| Full suite 339/339 | **CONDITIONAL** | Finalizer must re-run after critique repairs |
| Executor NEXT month-end flow PCS | **REJECT / SUPERSEDE** | Calendar thrash risk vs closed TOM/OPEX/momentum |
| Readiness LATEST NEXT still `THETA_EXPRESSIONS_...` | **STALE** | That seed was consumed by this wake; patch NEXT only |

## Finalizer actions (required / optional)

Required:
1. Preserve `FAMILY_CLOSED` and quarantine key `POST_SHOCK_RANGE_COMPRESSION_MATCHED_CONTROL` (exact panel/trigger/control/horizon).
2. Replace living NEXT with merged seed (do not leave month-end calendar flow as the sole seed).
3. Re-run focused post-shock + low_hv provenance tests and full suite; keep claim JSON immutable unless a claim-invalidating defect appears (none found).
4. Promote durable lesson: post-shock high-HV near-SMA20 cohort expanded vs matched high-HV quiet controls over five sessions — gross mechanism fails before short-gamma iron butterfly can help.
5. Update readiness surfaces for closed family + NEXT; do not change phase/B capital path.
6. Complete deterministic integration only after verification green.

Optional polish:
- Ensure wake prose never implies 21-DTE theta/vega was measured.
- Record no-advance streak=2 / pivot-required for next orientation after integration.

## Hard stops observed

No live orders, broker session, shadow/live promote, agentic arm, secrets in git, or main-account trading. Challenger did not evolve --apply.

MOA_CHALL_PARTIAL
