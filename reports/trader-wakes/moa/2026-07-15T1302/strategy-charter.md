# Strategy decision charter — 2026-07-15T1302

- Phase / claim: BUILD, L0 discovery only; Black-Scholes/listed-Friday proxy option marks over historical underlying bars. No L1, paper seat, observed-option, shadow, broker, arm, or live claim.
- Economic edge mechanism: a global one-open-risk-unit cadence policy can prevent correlated bullish put-credit-spread signals from stacking during the same adverse path, lowering portfolio drawdown without destroying the fixed stream's after-cost expectancy.
- Candidate / family scope: `ONE_RISK_UNIT_CADENCE_POLICY_F0` applied without signal or DNA retuning to the catalog-seed 14-DTE `put_credit_spread` stream on the fixed, outcome-rank-free set `AAPL, AMD, BAC, F, PLTR, SMCI, SOFI, TSLL`. Every symbol uses `STRUCTURE_CATALOG[put_credit_spread].config_seed`, one-bar lagged entry signals, one lot, and a $300 one-lot max-loss fail-close. The comparison is identical raw signals within each cost axis: uncapped independent positions versus deterministic symbol-priority admission while exactly one risk unit is open. Clusters are recorded (`semis`: AMD/SMCI; all others singleton); the global cap is stricter than the cluster cap.
- Current funnel stage: `F0_MECHANISM`.
- Chronology / partition: synchronize the fixed symbols on common 5-year daily-feature dates, reserve the final 40% as untouched holdout blueprints, and inspect only the first 60% train partition. No holdout option simulation or outcome metric is permitted in this wake.
- Cost axes: adverse 5% slippage and fixed $0.01 option-price half-spread per leg, evaluated independently.
- Predeclared falsifier: close the family unless the capped train stream (a) has at least 20 accepted trades on each cost axis, (b) is after-cost positive on each axis, (c) has close-to-close marked portfolio max drawdown <= $75 on each axis, (d) has one-lot worst-case loss <= $300 and observed maximum concurrency exactly 1, (e) retains at least 75% of the uncapped positive per-trade expectancy on each axis, and (f) reduces marked max drawdown by at least 25% versus the otherwise-identical uncapped stream on each axis. Any integrity, lag, population, determinism, ledger, or holdout-read violation also falsifies.
- Exact decision this wake closes: `STRATEGY_ADVANCED` from F0 to F1 if every predeclared train gate passes; otherwise `FAMILY_CLOSED` at F0 with the dominant failure recorded and unchanged cadence-policy reruns quarantined.
- Selection-bias boundary: the stream uses catalog defaults and a fixed prior multi-symbol set, not registry PnL ranks, latest winners, or post-result parameter selection. Symbol priority is the listed fixed order and cannot use outcomes.
- Capital shape: one-lot defined-risk put-credit spreads only; each accepted trade must report `capital_fit_usd` and `max_loss_usd` from its defined maximum loss, both <= $300, with operating `max_lots=1` for the $3,000 sleeve.

Prior NEXT is taken because the active epoch reset makes the portfolio-policy evidence class both novel and open; it supersedes more adjacent signal-filter volume and does not reopen any closed signal family by retuning its DNA.
