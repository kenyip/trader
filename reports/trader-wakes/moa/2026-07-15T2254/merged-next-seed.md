# Merged NEXT seed — 2026-07-15T2254

Source: executor closeout + Grok 4.5 challenger (PASS WITH NITS)

## ONE NEXT

`TSLL_OBSERVED_TERM_CARRY_DATA_OR_MATERIALLY_DIFFERENT_L0_PIVOT`

## Branch conditions

Use exactly one branch per wake:

1. **Distinct weekday-RTH and frozen observed floor unmet**
   (`>=12` distinct weekday-RTH dates, `>=3` short-expiry cycles, `>=20`
   non-overlapping eligible paths, `>=8` frozen one-to-one controls):
   append one provenance-safe all-expiration TSLL snapshot and report only
   density / path / admission / control counters. Schema-v2 `EVIDENCE_WAIT` with
   `evidence_wait_reaffirmation=true` does not increment the no-advance streak.

2. **Off-hours / strategy decision wake:**
   `strategy_pivot_required=true` after two completed epoch no-advance decisions.
   Choose a materially different non-quarantined economic mechanism or evidence
   class **outside**:
   - sector-breadth / participation-thrust directional continuation
   - exact closed family `BROAD_SECTOR_BREADTH_THRUST_FORWARD_DRIFT` and nearby
     same-panel threshold/horizon/SMA retunes
   - other integrated closed families already in orientation
   Complete a full Layered Edge Stack and predeclared falsifier before outcomes.

3. **Observed floor met:**
   evaluate exact parked `TSLL_OBSERVED_TERM_CARRY_DIAGONAL_V1` development 60%
   only; keep final 40% unread; no Black-Scholes proxy substitution for the
   observed claim; `$300` remains admission budget, not proven structural max loss.

## Hard bans

No same-date archive churn; no unchanged breadth-thrust retune; no holdout open for
the closed breadth family; no registry/paper force; no shadow/arm/broker/funding/live.

## Epoch context (accepted)

- Active epoch started `2026-07-15T2152`
- After this wake: completed strategy decisions 2; consecutive no-advance 2
- `strategy_pivot_required=true`
- `strategy_burst_stop_required=false`
- Pure distinct-RTH append reaffirmations remain streak-exempt
