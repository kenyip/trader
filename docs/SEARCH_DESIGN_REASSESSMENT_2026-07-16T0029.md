# Search-design reassessment — 2026-07-16T0029

Phase: BUILD / L0 research only
Sleeve: $3,000 Agentic
Trigger: three counted no-advance decisions in active epoch `TSLL_OBSERVED_TERM_CARRY_DIAGONAL_V1`
Decision: repair false living-candidate state before any successor search volume

## Why this is a reassessment, not a fourth strategy search

The 2152 observed TSLL diagonal is evidence-bound at F0, the 2254 breadth-thrust direction-up mechanism closed at F0, and the 2344 residual-reversal mechanism closed at F0 on its frozen tail gate. The required burst stop is therefore active. This wake did not mine another panel, threshold, structure, or holdout.

Orientation exposed a more fundamental routing defect: integrated readiness correctly said there was no living leader, while `scripts/trader_build_progress.py` derived two living candidates for the same `MULTINAME_BREAKOUT_BULL_CALL_14D_V1` lineage. Both were historical F1/F2 descriptive scopes. The exact option expression had been closed at 1648, but `closed_families` used a compact candidate ID plus qualifier, so full-string equality did not retire the earlier prose scopes. The external monitor then emitted `PAUSE_SUCCESS_LIVING`, which could freeze discovery on a strategy that failed 16 conjunctive option gates.

This was claim-invalidating because candidate lifecycle state controls search orientation. A false living F2 row is not harmless reporting noise: it can misstate convergence, create a stale leader, and stop a valid successor epoch.

## Repair and dependent retest

The progress derivation now reconciles exact legacy strings plus whole uppercase underscore lineage IDs. Closure removes every earlier descriptive scope sharing the exact candidate token. Whole-token matching does not use prefixes; the negative control proves closing `ALPHA_BREAKOUT_V10` does not close `ALPHA_BREAKOUT_V10_NEW`.

Current integrated history now derives:

- living candidates: `0`;
- furthest living stage: none;
- `MULTINAME_BREAKOUT_BULL_CALL_14D_V1` present in closed lineage keys;
- active-epoch no-advance streak: `3`;
- monitor disposition: continue/reassess, not false success-on-living.

The dependent exact breakout bull-call option experiment was rerun under current code without DNA changes. Artifact `.cache/platform/breakout_bull_call_option_2026-07-16T0029.json` is substantively identical to v7 after excluding only `generated_at`; normalized SHA-256 is `b32c951ff607a0f2262005634191bd94c6fd36bc599338ec1a34e599b3ba166e`. It remains `FAMILY_CLOSED` with 16 failed checks, `capital_fit_usd=92.56501618263479`, one-lot `max_loss_usd=246.90032488027197`, and `max_lots=1`. Capital fit does not rescue failed dual-cost expectancy, drawdown, density, or secondary support.

Compact machine evidence: `reports/trader-wakes/moa/2026-07-16T0029/lineage-retest-summary.json`.

## Search-design conclusions

1. Candidate identity must be lifecycle-stable. Prose scope is audit context, not a primary key.
2. A living strategy is the latest reconciled lifecycle state, not the count of all historical advances.
3. Advancement and later option-expression closure must not coexist as duplicate seats for the same candidate ID.
4. A parked candidate-scoped evidence wait remains distinct from a living advanced candidate.
5. The active epoch remains burst-stopped. This repair removes a false success pause; it does not create a strategy advance or reset the streak.
6. Proxy discovery remains open after reassessment, but the next epoch must be materially different and must not reopen the closed breakout expression, breadth thrust, or residual-reversal family.

## Successor design recommendation

The next highest-information evidence class is a prior-known scheduled macro event rather than another outcome-mined stock panel: use official FOMC decision dates and prior-completed SPY state to test post-announcement policy-information resolution against prior-only matched non-event controls. The mechanism is policy-information underreaction/resolution, not issuer earnings drift, generic turn-of-month/OPEX flow, sector breadth, peer residual reversal, or post-single-stock shock compression. Begin with underlying F0 train only; before reading outcomes freeze the official schedule and published-time session map, next-completed-session entry, a signed five-session SPY return with the positive-direction hypothesis fixed rather than flippable after inspection, prior-only same-regime no-reuse controls, dependence-aware uncertainty/tail/density gates, and a chronological sealed unread holdout. Ambiguous announcement times and unsupported controls stand aside. A future one-lot `$2` bull-call debit spread is structural context only (`capital_fit_usd=$200`, frictionless `max_loss_usd=$200` before closing friction, `max_lots=1`) until the underlying mechanism survives and a separate option-payoff stage is predeclared. No option pricing occurs at F0 and no proxy result can earn L1.

## Exactly one NEXT seed

`FOMC_INFORMATION_RESOLUTION_SPY_DIRECTION_F0`: open one successor epoch around official prior-known FOMC decision dates; before outcomes freeze the official schedule/published-time session map, ambiguous-time stand-aside, next-completed-session entry, signed five-session SPY return with positive direction predeclared and no post-hoc short/two-sided flip, prior-only same-regime matched controls with no reuse/substitution, chronological train/sealed unread holdout, dependence-aware uncertainty/tail/density and control-support gates, and zero option pricing at F0. Keep it distinct from closed issuer post-earnings drift. Conditional future structure is one-lot 18–24 DTE `$2` bull-call debit spread with `capital_fit_usd=$200`, frictionless `max_loss_usd=$200` before closing friction, `max_lots=1`; no OPEX/TOM/breadth/residual/breakout retune, registry, paper force, shadow, arm, broker, or live action.
